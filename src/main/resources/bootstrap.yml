
spring:
  application:
    name: report-service
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888

management:
  endpoints:
    web:
      exposure:
        include: '*'

topic:
  movement:
    name: MovementTopic


